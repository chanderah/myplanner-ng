export interface UserConfig {
    // ripple: false,
    // inputStyle: "outlined",
    // menuMode: "static",
    // colorScheme: "light",
    // theme: "lara-light-indigo",
    // scale: 14
    ripple: boolean;
    inputStyle: string;
    menuMode: string;
    colorScheme: string;
    theme: string;
    scale: number;
    tz: string;
}
